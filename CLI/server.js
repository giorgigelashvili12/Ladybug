class LadybugServer {
  static applyConfig(serverChoice) {
    console.log(`Applying server config: ${serverChoice}`);
    console.log(`Server config ${serverChoice} applied.`);
  }
}

module.exports = LadybugServer;